# Security

`dsh-wsl-tray` must not log, print, or return secrets (tokens, passwords, PEM keys).
Launcher scripts only start `dsh web` in the configured WSL distro.

Report issues via GitHub Issues on this repository.
