# Changelog

## 0.1.0

- Initial public release of `dsh-wsl-tray` for DeepSeek Harness on Windows + WSL.
- Tool `wsl_tray`: `status`, `write_launcher`, `open_shortcut_dir` (scripts only, no tray install).
